{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "close_transaction_account",
  "type": "object",
  "x-group": "B",
  "x-client-resolution": "preserve-original-id-or-refuse",
  "x-http": {
    "method": "POST",
    "path": "/api/v1/documents",
    "notes": "Feature 027: identical client-preservation-or-refusal behavior to create_credit_note/create_receipt (same group, same rule - REQ-INV-012/013) - the 6th tool with this exact bug, not in the original 5 named by the problem description, added to scope 2026-08-06 for consistency (Clarifications). Only the document type (320, closing combo) and only-supports-type-300-originals check differ (unchanged from Feature 023)."
  },
  "properties": {
    "original_invoice_id": {"type": "string", "description": "Morning document id of the transaction account being closed. Its client sub-object must have a real id or this tool refuses (REQ-INV-013)."},
    "amount": {"type": "number", "minimum": 0, "description": "Optional override - defaults to the original's full total."},
    "description": {"type": "string"},
    "vat_included": {"type": "boolean", "default": true}
  },
  "required": ["original_invoice_id"],
  "x-approval": "always"
}
