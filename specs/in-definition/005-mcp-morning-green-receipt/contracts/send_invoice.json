{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "send_invoice",
  "type": "object",
  "x-http": {
    "method": "POST",
    "path": "/api/v1/documents/{id}/send",
    "notes": "Send invoice to client (email/SMS). Postman collection demonstrates sending via document actions; use document id in path."
  },
  "properties": {
    "invoice_id": {"type": "string"},
    "phone_number": {"type": "string"},
    "message": {"type": "string"}
  },
  "required": ["invoice_id"]
}
