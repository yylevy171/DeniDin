{
  "$id": "https://denidin.example/schemas/mcp/get_status.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Get Parse Status",
  "type": "object",
  "x-http": {
    "method": "GET",
    "path": "/api/v1/documents/{id}",
    "notes": "Get document status and parsed metadata by document id (maps to GET /api/v1/documents/{id} in Postman collection)."
  },
  "properties": {
    "id": {"type": "string"}
  },
  "required": ["id"],
  "response_example": {"id": "123", "status": "parsed", "parsed_metadata": {"foo":"bar"}}
}
