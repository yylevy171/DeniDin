# Capabilities Sanity — the 10 tests (T1–T10)

Reconstructed from the actual sanctioned run (`_capabilities_sanity_n5_flagoff_20260915_183255.txt`,
run via `scripts/run_sanity_parallel.sh`) plus the substitutions you asked for (T1/T2/T10 swapped
in: bank deposit image, client add, create combo invoice). This is the authoritative list — use
these exact node ids and this exact numbering from now on, nothing else.

| # | Capability (Backbone tag) | Prompt lives at | Test node id | flag=OFF | flag=ON |
|---|---|---|---|---|---|
| **T1** | `media_analysis` | `config/prompts/capabilities/media_analysis.md` | `tests/expensive/test_ledger_event_capture_e2e.py::TestLedgerEventCaptureE2E::test_given_real_bank_deposit_image_then_full_fields_correctly_persisted` | ✅ PASSED | ❌ **FAILED** — English fallback bug, root cause found (Planning wrongly adds a `media_analysis` step), fix not yet applied |
| **T2** | `invoicing_write` (add client) | `config/prompts/capabilities/invoicing_write.md` | `tests/billed/test_denidin_morning_invoice_creation_e2e.py::test_godfather_add_client_requires_approval` | ✅ PASSED | ⬜ not yet run |
| **T3** | `reminders_write` (create) | `config/prompts/capabilities/reminders_write.md` | `tests/billed/test_reminder_lifecycle_billed.py::TestReminderLifecycleBilled::test_godfather_creates_one_time_reminder_button_approval` | ✅ PASSED | ✅ **PASSED** |
| **T4** | `reminders_write` (modify) | `config/prompts/capabilities/reminders_write.md` | `tests/billed/test_reminder_lifecycle_billed.py::TestReminderLifecycleBilled::test_modify_single_occurrence_of_recurring_reminder` | ✅ PASSED | ✅ **PASSED** |
| **T5** | `invoicing_read` (list) | `config/prompts/capabilities/invoicing_read.md` | `tests/billed/test_denidin_morning_list_invoices_e2e.py::test_godfather_lists_invoices_via_whatsapp` | ✅ PASSED | ✅ **PASSED** (after 3 real fixes: `_resolve_backbone_tool_calls` now loops instead of one fixed round; mcp_calls now accumulated across every round, not just the last; `ai_handler.last_response` now set on every flag-on dispatch path) |
| **T6** | `ledger_query` | `config/prompts/capabilities/ledger_query.md` | `tests/billed/test_ledger_query_billed.py::TestLedgerQueryBilled::test_hours_by_client_last_month` | ✅ PASSED | ✅ **PASSED** (after telemetry-wiring fix) |
| **T7** | `invoicing_write` (create invoice) | `config/prompts/capabilities/invoicing_write.md` | `tests/billed/test_denidin_morning_invoice_creation_e2e.py::test_godfather_creates_invoice_via_whatsapp_button_tap` | ✅ PASSED | ✅ **PASSED (5th run)**, after 2 real bugs fixed + 1 test-prompt clarification: (1) `denidin.py`'s button-tap dispatch called `backbone_orchestrator.resolve_button_tap(...)` with NO `request=` arg, so `invoicing.handler.resolve_button_tap`'s `_resolve_approval` (needs `request.model`/`max_tokens`) always hit its `request is None` branch and returned "⚠️ הפעולה נכשלה." without attempting the real call — `reminders.handler`'s own `resolve_button_tap` doesn't need `request`, which is why T3/T4 never caught this; fixed by building a real synthetic `AIRequest` in `denidin.py` (mirrors `AIHandler.resolve_button_tap`). (2) A run in between hit real, non-deterministic model behavior — the model asked a legitimate VAT-treatment clarifying question for `create_invoice` (which does have an optional `vat_included` field, confirmed against `morning-mcp-app`'s tool schema) instead of proceeding in one turn — not a code/prompt bug, since neither prompt (flag=on or flag=off) states a rule either way for this tool; per user direction, the test's ASK-turn phrasing was updated to state "לא כולל מע\"מ" explicitly so the scenario is unambiguous, same spirit as the rest of the test's phrasing. (3) real bug #9 found+fixed: even after the invoice was genuinely created, `ai_response.mcp_calls` was empty (`_resolve_approval`'s own follow-up response was never scanned for `mcp_call` items — a code path outside `call_capability_step`/`_resolve_backbone_tool_calls`, the only 2 places doing that extraction) — fixed by having `_resolve_approval` also call `orchestrator._extract_mcp_call_items` and threading `mcp_calls` through both `resolve_button_tap` and `resolve_typed_reply`'s returned `AIResponse`. All 1716 unit+integration tests + pylint 10.00/10 + mypy clean, verified before this final passing run. |
| **T8** | `ledger_capture` (text) | `config/prompts/capabilities/ledger_capture.md` | `tests/billed/test_ledger_event_capture_text_billed.py::TestLedgerEventCaptureTextBilled::test_given_real_gilyan_davidian_agreement_text_when_processed_then_captured_per_component` | ❌ FAILED in batch → ✅ **PASSED** on solo rerun via `run_single_test.sh` | ⬜ not yet run |
| **T9** | `media_analysis` (multi-component) | `config/prompts/capabilities/media_analysis.md` | `tests/expensive/test_ledger_event_capture_e2e.py::TestLedgerEventCaptureE2E::test_given_real_multi_component_agreement_image_then_components_correctly_persisted` | ✅ PASSED | ⬜ not yet run |
| **T10** | `invoicing_write` (combo document) | `config/prompts/capabilities/invoicing_write.md` | `tests/billed/test_denidin_morning_document_creation_e2e.py::test_godfather_creates_combo_document_via_whatsapp` | ✅ PASSED | ❌ **FAILED (3rd investigated run)** — 3 real bugs found+fixed so far: (1) media_analysis-for-plain-text — fixed with a code-level safety net in `orchestrator._execute_plan` that drops a planned `media_analysis` step outright when `turn_context["media"]` is None, regardless of what Planning said. (2) `INVOICING_WRITE`'s catalog description (`capability_tags.py`) never mentioned combo documents at all — fixed, rewritten to name them explicitly and clarify the write-vs-ledger-capture split. (3) **structural bug**: `propose_write` (`invoicing/handler.py`) was the ONLY domain capability handler that bypassed `orchestrator.call_capability_step` — it made its own bare API call with just the current message, never `self._turn_conversation_history` (the real verbatim rolling-window transcript every other capability gets) — so multi-turn invoicing flows had to rely entirely on Intent Identification's lossy one-line paraphrase for continuity, explaining why a stated default (payment_method→bank_transfer) got asked about anyway. Fixed: `propose_write` now builds `input` the same way `call_capability_step` does (history + BACKBONE_TOOLS + backbone-tool-call resolution), verified working (turn 4's reply correctly synthesized "13 ש״ח כולל מע״מ...לאורה פדורוב...בתאריך היום" from history). All 1716/1716 unit+integration pass (0 pre-existing exemptions — the `test_edited_deleted_webhook_routing.py` flake was root-caused and fixed too: it reused a shared, non-unique `CHAT_ID` against Feature 070's genuinely-persistent session store, accumulating duplicate notes across repeated suite runs — given its own fresh, unique chat id like the rest of the file's tests already use), pylint 10.00/10, mypy clean on touched files. **New, deeper issue surfaced on re-run**: Planning misrouted the DESCRIBE and DATE follow-up turns to `reminders_read` ("אין תזכורות פעילות כרגע") instead of continuing the invoicing_write flow — Planning has no notion of "already mid-flow in a pending invoicing_write conversation," re-deriving the capability fresh each turn from Intent Identification's paraphrase alone. By turn 4, invoicing_write DID correctly run with full history (proving fix #3 works) but then self-contradictorily asked for "the exact date of today in DD/MM/YYYY format" in the same message where it had just stated "בתאריך היום" — not resolving its own already-injected current-date context. This is a bigger architectural gap (mid-flow routing continuity), not a single-file fix — stopped to report before further investigation. |

`intent_identification` and `planning` (the 2 meta-capabilities, `config/prompts/backbone.md` +
`config/prompts/capabilities/intent_identification.md` / `planning.md`) are not directly
testable on their own — every one of T1–T10 exercises both on every turn.

## Status right now
- **flag=OFF sweep: all 10/10 confirmed passing** (T1, T3–T7, T9, T10 in the original batch; T2
  in the batch; T8 failed in the parallel batch, then passed cleanly solo).
- **flag=ON sweep: only T1 has been run.** It FAILED — real bug, root cause found (Planning's
  prompt has no signal telling it whether media is actually attached this turn, and its own
  example text biases it toward adding a `media_analysis` step even for plain text). Fix not yet
  applied — awaiting your go-ahead on the proposed approach.
- T2–T10 under flag=ON: **not started.**
